<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Аппендеры -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/application.log</file>                     <!-- Файл для логов -->
        <append>true</append>                                  <!-- Добавляем новые записи к старым -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender"> <!-- Консольный аппендер -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Корень уровня логирования -->
    <root level="DEBUG">
        <appender-ref ref="STDOUT"/>                          <!-- Записываем в консоль -->
        <appender-ref ref="FILE"/>                            <!-- Записываем в файл -->
    </root>

</configuration>